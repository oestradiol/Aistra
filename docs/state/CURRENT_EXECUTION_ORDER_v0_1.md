# Current Execution Order v0.8

## Purpose
Use this file when the question is not “what is everything in this package?” but “what do we do next without drifting?”

For current package posture, use `PROJECT_STATUS_v0_1.md`.
For the authoritative file map, use `../../governance/AUTHORITATIVE_INDEX_v0_1.md`.

## Live execution rule

Do now:
- stop ontology expansion unless a clean pilot forces it
- treat Route 1 as the active workstream
- use only the current local app or current ops runner for Route 1
- treat the baseline-explicit packet as the scored calibration surface

Do next:
- run additional clean Route 1 pilots with the current package
- collect exported results and reports without manual operator paraphrase
- inspect failures by field, especially source/certainty/contour

Do after that:
- decide whether the observed errors are prompt-design errors, scoring-policy errors, or ontology errors
- only then consider bounded revisions

Only then:
- reopen Route 2 human work
- open naturalistic baseline as a separate exploratory mode
- consider broader validation or publication framing
